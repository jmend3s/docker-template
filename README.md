# docker

This is a template folder for the docker related scripts to include in projects.
To use, fork the repo and pull it to the root of the project, replace the place holders with the values for the project and ./run.sh
